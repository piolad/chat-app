systemLog:
  verbosity: 0  # General log verbosity
  component:
    storage:
      verbosity: 0  # Reduce storage-related logging
    accessControl:
      verbosity: 0  # Reduce access control logs
    command:
      verbosity: 0  # Reduce command logs
